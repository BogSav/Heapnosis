#pragma once

#include <algorithm>
#include <cstddef>
#include <functional>
#include <iterator>
#include <vector>

namespace algorithms
{

/// @brief Bubble sort [first, last) in-place.
template <typename It>
void BubbleSort(It first, It last)
{
	// TODO: implement
	(void)first;
	(void)last;
}

/// @brief Selection sort [first, last) in-place.
template <typename It>
void SelectionSort(It first, It last)
{
	// TODO: implement
	(void)first;
	(void)last;
}

/// @brief Insertion sort [first, last) in-place.
template <typename It>
void InsertionSort(It first, It last)
{
	// TODO: implement
	(void)first;
	(void)last;
}

/// @brief Shell sort [first, last) in-place.
template <typename It>
void ShellSort(It first, It last)
{
	// TODO: implement
	(void)first;
	(void)last;
}

/// @brief Merge sort [first, last) in-place (or with auxiliary storage).
template <typename It>
void MergeSort(It first, It last)
{
	// TODO: implement
	(void)first;
	(void)last;
}

/// @brief Quick sort [first, last) in-place.
template <typename It>
void QuickSort(It first, It last)
{
	// TODO: implement
	(void)first;
	(void)last;
}

/// @brief Bucket sort for a range of integers or floats.
/// @details Only works on numeric types. Sorts [first, last) in-place.
template <typename It>
void BucketSort(It first, It last)
{
	// TODO: implement
	(void)first;
	(void)last;
}

/// @brief Counting sort for non-negative integers in [first, last).
template <typename It>
void CountingSort(It first, It last)
{
	// TODO: implement
	(void)first;
	(void)last;
}

/// @brief Radix sort (LSD) for non-negative integers in [first, last).
template <typename It>
void RadixSort(It first, It last)
{
	// TODO: implement
	(void)first;
	(void)last;
}

/// @brief Heap sort [first, last) in-place.
template <typename It>
void HeapSort(It first, It last)
{
	// TODO: implement
	(void)first;
	(void)last;
}

}  // namespace algorithms
